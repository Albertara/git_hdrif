import time
from math import *


#Liður 1
"""
s = 1
while(s == 1):
	t1 = int(input("Sláðu inn heiltölu: "))
	mod = t1 % 5
	if mod == 0:
		print("Þessi tala er í 5 sinnum töflunni")
		aftur1 = str(input("Viltu skrifa inn aðra fallega heiltölu? (Já/nei)"))
		if aftur1 == "já":
			s = 1
		else:
			s = 0

	else:
		print("Talan er ekki í 5 sinnum töflunni")


#Liður 2


f = 1
hlaupari = 0
while(f == 1):
	ar = int(input("Sláðu inn ártal: "))
	armod = ar % 4
	if armod == 0:
		hlaupari = 1
		öld = ar % 100
		if öld == 0:
			hlaupari = 0
			hlaup = ar % 400
			if hlaup == 0:
				hlaupari = 1
	if hlaupari == 1:
		print (ar, "er hlaupár")
		aftur2 = str(input("Viltu skrifa inn annað ártal? (já/nei) "))
		if aftur2 == "já" or aftur2 == "ja":
			f = 1
		else:
			f = 0
	elif hlaupari == 0:
		print (ar, "er ekki hlaupár")
		aftur2 = str(input("Viltu skrifa inn annað ártal? (já/nei)"))
		if aftur2 == "já" or aftur2 == "ja":
			f = 1
		else:
			f = 0

"""

#Liður 3